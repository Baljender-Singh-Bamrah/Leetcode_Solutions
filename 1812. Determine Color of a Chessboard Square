class Solution {
public:
    bool squareIsWhite(string c) {
        vector<int> even = {0, 1,0,1,0,1,0,1,0}, odd = {0, 0,1,0,1,0,1,0,1};
        int row = c[0]-'a'+1, col = c[1] - '0';
        if(row%2) {
            return odd[col];
        }
        return even[col];
    }
};
