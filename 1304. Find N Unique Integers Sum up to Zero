class Solution {
public:
    vector<int> sumZero(int n) {
        vector<int> res;
        while(n>1) {
            res.push_back(-n);
            res.push_back(n);
            n-=2;
        }
        if(n) res.push_back(0);
        
        return res;
    }
};
