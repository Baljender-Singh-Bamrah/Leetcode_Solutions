class Solution {
public:
    int maxJump(vector<int>& nums) {
        int dist = nums[1] - nums[0];

        for(int i=2 ; i<nums.size() ; i++) {
            dist = max(dist, nums[i] - nums[i-2]);
        }

        return dist;
    }
};
