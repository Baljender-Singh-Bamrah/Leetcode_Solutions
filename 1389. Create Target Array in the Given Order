class Solution {
public:
    vector<int> createTargetArray(vector<int>& nums, vector<int>& idx) {
        vector<int> result;
        for(int i=0 ; i<nums.size() ; i++)
        {
            result.insert(result.begin()+idx[i],nums[i]);
        }
        return result;
    }
};
